import { StyleSheet } from 'react-native';



export const dayItemStyles = StyleSheet.create({
    container: {
        height: 120
    },
    contentContainer: {
        height: 110
    },
})

export const forecastStyles = StyleSheet.create({
    container: {
        width: '90%', 
        height: '60%'
    },
})